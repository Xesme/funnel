/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FunnelPipe } from './funnel.pipe';

describe('FunnelPipe', () => {
  it('create an instance', () => {
    const pipe = new FunnelPipe();
    expect(pipe).toBeTruthy();
  });
});
